import user from '../user/user_action';
import checkin from '../checkin/checkin_action';
import main from '../main/main_action';


const allActions = {
  user: user,
  main: main,
  checkin: checkin,

};

export default allActions;
